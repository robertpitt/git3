name: Release
on:
  push:
    tags: ['v[0-9]+.[0-9]+.[0-9]+']
permissions:
  contents: write
  id-token: write
  attestations: write

env:
  GOTOOLCHAIN: local
  STATICCHECK_VERSION: v0.8.1
  GOVULNCHECK_VERSION: v1.7.0

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: {fetch-depth: 0}
      - name: Verify signed release tag
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag_type=$(gh api "repos/${GITHUB_REPOSITORY}/git/ref/tags/${GITHUB_REF_NAME}" --jq '.object.type')
          if [[ "${tag_type}" != "tag" ]]; then
            echo "::error::${GITHUB_REF_NAME} must be an annotated, signed tag"
            exit 1
          fi
          tag_sha=$(gh api "repos/${GITHUB_REPOSITORY}/git/ref/tags/${GITHUB_REF_NAME}" --jq '.object.sha')
          verified=$(gh api "repos/${GITHUB_REPOSITORY}/git/tags/${tag_sha}" --jq '.verification.verified')
          reason=$(gh api "repos/${GITHUB_REPOSITORY}/git/tags/${tag_sha}" --jq '.verification.reason')
          if [[ "${verified}" != "true" ]]; then
            echo "::error::${GITHUB_REF_NAME} signature is not verified (${reason})"
            exit 1
          fi
      - uses: actions/setup-go@v6
        with: {go-version-file: go.mod, cache: true}
      - run: test -z "$(gofmt -l .)" && go test -race ./... && go vet ./...
      - run: go install honnef.co/go/tools/cmd/staticcheck@${STATICCHECK_VERSION} && staticcheck ./...
      - run: go install golang.org/x/vuln/cmd/govulncheck@${GOVULNCHECK_VERSION} && govulncheck ./...
  build:
    needs: test
    strategy:
      matrix:
        os: [linux, darwin]
        arch: [amd64, arm64]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with: {go-version-file: go.mod, cache: true}
      - name: Build deterministic archive
        env:
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
          CGO_ENABLED: '0'
          VERSION: ${{ github.ref_name }}
        run: |
          mkdir dist
          SOURCE_DATE_EPOCH=$(git log -1 --format=%ct)
          export SOURCE_DATE_EPOCH
          go build -trimpath -ldflags "-s -w -X main.version=${VERSION} -X main.commit=${GITHUB_SHA} -X main.buildTime=$(date -u -d @${SOURCE_DATE_EPOCH} +%Y-%m-%dT%H:%M:%SZ) -X main.dirty=false" -o dist/git3 ./cmd/git3
          cp LICENSE NOTICE dist/
          tar --sort=name --mtime="@${SOURCE_DATE_EPOCH}" --owner=0 --group=0 --numeric-owner -C dist -cf - git3 LICENSE NOTICE | gzip -n > "git3_${VERSION#v}_${GOOS}_${GOARCH}.tar.gz"
      - uses: actions/upload-artifact@v4
        with:
          name: git3-${{ matrix.os }}-${{ matrix.arch }}
          path: git3_*.tar.gz
  publish:
    needs: build
    runs-on: ubuntu-latest
    environment: release
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v4
        with: {pattern: 'git3-*', merge-multiple: true}
      - run: sha256sum git3_*.tar.gz | sort -k2 > checksums.txt
      - uses: sigstore/cosign-installer@v3
      - run: cosign sign-blob --yes --bundle checksums.txt.sigstore.json checksums.txt
      - run: for f in git3_*.tar.gz; do cosign sign-blob --yes --bundle "$f.sigstore.json" "$f"; done
      - uses: actions/attest-build-provenance@v2
        with: {subject-path: 'git3_*.tar.gz'}
      - env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" --verify-tag --generate-notes git3_*.tar.gz *.sigstore.json checksums.txt install.sh
